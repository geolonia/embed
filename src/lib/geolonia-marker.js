import mapboxgl from 'mapbox-gl'

/**
 * Render the map
 *
 * @param container
 */
export default class GeoloniaMarker extends mapboxgl.Marker {
  constructor(options) {

    super(options)
    const marker = this
  }
}
